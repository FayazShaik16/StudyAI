import * as React from 'react'; import { cn } from '@/utils/utils'; export const Loader = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(({ className, ...props }, ref) => ( <div ref={ref} className={cn('', className)} {...props} /> )); Loader.displayName = 'Loader';
