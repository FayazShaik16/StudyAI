import * as React from 'react'; import { cn } from '@/utils/utils'; export const Radio = React.forwardRef<HTMLDivElement, React.HTMLAttributes<HTMLDivElement>>(({ className, ...props }, ref) => ( <div ref={ref} className={cn('', className)} {...props} /> )); Radio.displayName = 'Radio';
